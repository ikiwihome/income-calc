# AGENTS.md

本文件用于约束在本仓库中进行 Vibe Coding 的 AI Agent 和协作者。目标是在快速迭代的同时，守住税务计算正确性、界面一致性和可验证性。

## 项目目标

这是一个上海工资税后收入计算器。用户通过四步向导录入工资、五险一金和专项扣除，查看逐月实发、年度汇算与最终到手收入，并可导出 Excel。

优先级依次为：

1. 计算结果正确且可解释。
2. 用户输入不会被意外覆盖或丢失。
3. 桌面端与移动端均清晰、可操作、无溢出。
4. 代码保持简单、类型安全、容易继续迭代。

## 技术约定

- 使用 Nuxt 4、Vue 3、TypeScript 和 `<script setup lang="ts">`。
- 样式由 `app/assets/css/tailwind.css` 和 `app/assets/css/salary-calculator.css` 管理。
- 图标使用 `@lucide/vue`，不要用 Emoji 或手绘 Unicode 图标代替界面图标。
- 包管理器固定为 pnpm，不要提交 npm 或 Yarn 锁文件。
- 优先复用现有组件、组合式函数和设计 token，不要引入新的 UI 框架。

## 文件职责

- `app/utils/taxCalculator.ts`：纯计算逻辑。不要在这里访问 DOM、浏览器 API 或 Vue 状态。
- `app/utils/shanghaiPolicy.ts`：按年度维护上海政策参数。
- `app/composables/useSalaryCalculator.ts`：管理表单状态、派生结果和步骤操作。
- `app/utils/exportSalaryWorkbook.ts`：Excel 工作簿结构、格式和下载逻辑。
- `app/components/SalaryCalculator.vue`：主向导结构，不复制计算公式。
- `app/components/SalaryCalculatorLiveEstimate.vue`：只展示派生数据。
- `app/assets/css/salary-calculator.css`：页面视觉系统和响应式规则。

## 计算规则

- 金额计算保留完整数值，展示时再格式化；不要在中间步骤因 UI 精度提前取整。
- 个税继续使用累计预扣逻辑，本月税额必须扣除此前已预扣税额。
- 社保与公积金基数分别应用政策上下限，不要假设两者始终相同。
- 1–6 月和 7–12 月可能使用不同政策参数，不能合并为单一年度基数。
- 住房租金与住房贷款利息互斥。
- 新增或修改扣除项时，同时检查月度申报、年度汇算、适用月份和上限。
- UI 文案不得承诺与税务机关结果完全一致。

## 界面约束

- 保持现有“现代工资单 / 收入台账”方向：深墨蓝、钴蓝和少量朱砂橙。
- 标题使用宋体角色，正文使用系统无衬线，金额使用等宽或数字字体。
- 避免新增大面积渐变、过多圆角卡片、胶囊按钮和无意义装饰。
- 交互控件必须保留可见焦点状态；动效必须遵守 `prefers-reduced-motion`。
- 结果页顶部概览保持吸顶；逐月表格不使用内部垂直滚动，仅在窄屏保留横向滚动。
- 至少检查 375px、641px、901px 和 1280px 宽度，页面不得产生整体横向滚动。
- 修改主要界面后更新 `img/screenshot.png`。

## 修改原则

- 先阅读相关文件和现有实现，再做最小范围修改。
- 不重写与任务无关的代码，不删除用户已有改动。
- 不把业务逻辑塞进模板表达式；复杂派生值使用 `computed` 或纯函数。
- 新增状态时明确默认值，并确认步骤前进、后退和重新计算后的行为。
- 政策参数变更需要注明适用年度与上下半年，不凭印象更新数据。
- 不编辑 `.nuxt/`、`.output/`、`node_modules/` 等生成目录。

## 完成标准

提交工作前至少运行：

```bash
pnpm lint
pnpm typecheck
pnpm build
```

涉及界面时，还需要在本地浏览器验证：

- 四个步骤均可进入和返回。
- 表单、展开区、吸顶区和操作栏无错位。
- 桌面端与移动端无页面级横向溢出。
- 浏览器控制台无新增错误。

涉及计算或政策时，应补充或执行针对边界值、半年切换和累计预扣的测试；若项目暂时缺少自动化测试，应在交付说明中明确列出手工验证场景。
